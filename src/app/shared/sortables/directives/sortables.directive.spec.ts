import { SortablesDirective } from './sortables.directive';

describe('SortablesDirective', () => {
  it('should create an instance', () => {
    const directive = new SortablesDirective();
    expect(directive).toBeTruthy();
  });
});
